// String Index
// it always start from  0 index and so on

let firstName = 'Hamza';
let stringIndex = firstName[2];
console.log(stringIndex)

// String length
// it always start from  1 index and so on and last index -1

let lastName = "Ali";
let lengthIndex = lastName.length;
let lengthIndexEndPoint = lastName[lastName.length - 1]; //to get last index
console.log(lengthIndexEndPoint);